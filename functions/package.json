{
  "name": "ppp-analyzer-functions",
  "version": "1.0.0",
  "engines": { "node": "20" },
  "main": "src/pipeline/index.js",
  "scripts": {
    "serve":  "firebase emulators:start --only functions,firestore,storage",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint "
  },
  "dependencies": {
    "firebase-admin":    "^12.0.0",
    "firebase-functions": "^4.9.0",
    "pdf-parse":         "^1.1.1",
    "mammoth":           "^1.7.1",
    "tesseract.js":      "^5.1.0",
    "pdf2pic":           "^3.1.1"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
